# Leave approval staging change log

Branch: `fix/staging-leave-first-approver-20260924`  
Base: `develop` at `694f629b1b6f58142a617ca2a6dc108605aa9527`  
Environment: OrionGroupStage only. This branch is not a production release.

| Time (UTC) | Category | Change | Reason | Verification / status |
| --- | --- | --- | --- | --- |
| 2026-09-24 15:15 | Server validation | Map the first leave approver to `custom_initial_approver_status` in the active approvals package and the legacy module. Initialize that field to `Open` on creation and amendment. | The application `status` stays `Open` during intermediate approval, so using it as the first approver result blocks the next approver and final submission. | Python syntax and focused sequence checks passed; staging workflow retest pending deployment. |
| 2026-09-24 15:15 | Reminders | Use the same first approver field in `leave_escalation.py`. | Prevent reminders from treating an already approved first stage as pending. | Python syntax checked; scheduler behavior pending staging retest. |
| 2026-09-24 15:15 | Form and cancellation | Use the same field in the form approval map and set it on draft creation/cancellation. | Keep button visibility, submission checks and cancellation consistent with server validation. | JavaScript syntax checked; role-specific form retest pending deployment. |
| 2026-09-24 15:15 | Initial scope | Retain `status` as the overall Leave Application result and preserve the existing department and management approval sequence. The later 16:05 request expanded creation roles and company scope. | Keep approval routing separate from initiation permissions. | Review the full diff before staging deployment. |
| 2026-09-24 15:20 | Repository verification | Confirmed in the authenticated Frappe Cloud `OrionGroupStage` Apps view that Orion-ERP uses `yousafaj/Orion-ERP`, branch `develop`, commit `694f629`. | Verify the exact staging source before publishing a branch. | Verified; the GitHub repository is public. |
| 2026-09-24 15:20 | Deployment gate | A push of this branch was rejected by automatic approval review because it would publish change details to a public repository. | Publishing to that repository has a disclosure consequence distinct from changing a private staging site. | Local commit prepared; no branch pushed and no staging or production deployment performed. |
| 2026-09-24 15:38 | Workflow alignment | Limit active approval rows to the configured workflow: four for Non-Office; three for most Office; two for Office HR/Management. Apply the same selection to server validation, notifications, escalation and form buttons. | Office records can contain an unused fourth approver field, which would block final submission; Office HR/Management skip the separate HR step. | Focused final-submission checks passed for all three routes, including rejection of missing active approvals. |
| 2026-09-24 16:05 | Non-Office initiators | Keep Employee Self Service, HR User and HR Manager creation grants. Add Operation Team read/write/create without submit and set Operation Manager create while retaining its submit grant. Restrict Operations' creation for other employees to Non-Office, including medical-certificate drafts and sending for approval. | Operations Team currently lacks Leave Application DocPerm; Operations Manager has create disabled. An unrestricted create grant would also cover Office employees. | Role/category checks passed locally; staging permissions and user sessions remain unchanged until deployment. |
| 2026-09-24 16:05 | Cross-company scope | Add Orion company User Permissions for Employee and Leave Application only to Operations users with existing Company restrictions, covering OG, OEST, OIFM and OBR. | Afsal currently has an OIFM-only Employee and Leave Application restriction, which would block the requested all-group initiation even with a create grant. OG currently has no Non-Office employees; Golden and Private are outside this scope. | Inspect permitted Employee choices for affected Operations users on staging after migration. |
| 2026-09-24 16:05 | Initiator/approver overlap | Let the document owner send a draft for approval; allow a designated approver to act even when also the employee. Continue enforcing the assigned approver and prior stages on the server. | The form formerly showed Send for Approval only to the employee; sent employee requests disabled Save even when the employee was an assigned approver. | Syntax and focused permission checks passed; confirm with signed-in staging users after deployment. |
| 2026-09-24 16:21 | Deployment metadata | Declare Frappe 15 compatibility in `pyproject.toml` on this feature branch. | Frappe Cloud refused the first branch-change attempt because the app did not advertise its compatible Frappe version. | Retry staging branch selection after publishing this commit; no app deployment occurred during the failed attempt. |
| 2026-09-24 17:05 | Staging migration correction | Import `add_user_permission` from `frappe.permissions` in the Operations permissions patch. | The first staging build succeeded, but migration failed because `frappe.add_user_permission` does not exist; Frappe Cloud recovered the site, leaving the new permissions unapplied. | Publish the corrective commit, rerun the staging deployment and verify the patch log and permissions. |

Staging already contains `custom_initial_approver_status` and a `Sync Leave Approval Workflow Statuses` server script that sets it to `Approved` as the workflow advances. The branch does not yet package creation of that custom field for another site. Before any future production proposal, include the field and reconcile the staging script with the app code.

The staging script currently marks `custom_status_approver1` Approved at Management Approval for Office HR/Management even though their workflow goes directly from the first HR Manager approval to Management Approval. Correct that audit-field behavior during staging UAT before sign-off; the app now excludes the skipped field from required approvals.

Staging acceptance: create fresh Office and Non-Office applications; have a self-service employee, an Operations Team user, an Operations Manager, an HR User (Jayshree), and an HR Manager create Non-Office applications. Confirm Operations can select Non-Office employees from each group company but cannot create a request for another Office employee. Confirm creators can send their drafts, approvals still follow assigned users and stage order, a creator who is also an assigned approver can approve, final approval submits, rejection works, and email plus in-app alerts reach the correct recipient. Separately diagnose the existing `Not Sent` email queue before any bulk resend.

Rollback: take a staging backup and record existing Operation Manager and Operation Team DocPerm and company User Permission rows before deployment. Redeploy the previous staging app ref from `develop`; restore the recorded permissions, removing only group-company User Permission rows added by this patch. Do not reset or rewrite `develop` or production.
